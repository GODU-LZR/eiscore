@startuml
title 图3-5 采购入库与来料检验活动图

start
:PMC/采购确认到货需求;
:采购人员维护采购订单/到货信息;
:仓管员登记到货;
:质检人员发起来料检验;

if (检验是否合格?) then (是)
  :生成采购入库单;
  :写入库存批次;
  :写入库存流水;
  :更新采购执行状态;
else (否)
  :记录不合格结果;
  :保留附件与处理意见;
  if (是否允许后续补录?) then (是)
    :保留待补录状态;
  else (否)
    :终止正式入库;
  endif
endif

:形成采购单-检验单-入库单关联;
stop

@enduml
