
services:
  # 1. 数据库
  db:
    image: postgres:16
    container_name: eiscore-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "123456"
      POSTGRES_DB: eiscore
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db_schema_and_data.sql:/docker-entrypoint-initdb.d/init.sql

  # 2. API 网关
  api:
    image: postgrest/postgrest
    container_name: eiscore-api
    ports:
      - "3000:3000"
    environment:
      PGRST_DB_URI: "postgres://postgres:123456@db:5432/eis_db"
      # 🟢 关键修改：列出所有需要生成 API 的 Schema
      # 注意：第一个 (public) 是默认 Schema，不加 header 时访问它
      PGRST_DB_SCHEMAS: "public, hr, scm" 
      PGRST_DB_ANON_ROLE: "web_anon"
      PGRST_JWT_SECRET: "my_super_secret_key_for_eiscore_system_2025"
    depends_on:
       - db

  # 3. Swagger 文档
  swagger:
    image: swaggerapi/swagger-ui
    container_name: eiscore-swagger
    restart: always
    ports:
      - "8079:8080"
    environment:
      # 🟢 关键修改 1：告诉 Swagger UI 它被托管在 /doc 路径下
      BASE_URL: /doc
      # 🟢 关键修改 2：使用相对路径请求 API，完美解决 CORS 和 Fetch Error
      API_URL: /api/
    depends_on:
      - api

  # 4. Nginx 网关
  nginx:
    image: nginx:alpine
    container_name: eiscore-nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/html:/usr/share/nginx/html
    depends_on:
      - api
      - swagger

volumes:
  pgdata: