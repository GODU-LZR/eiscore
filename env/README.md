# Env

此目录用于存放环境与初始化相关文件。

- .env：运行环境变量（已在根目录保留符号链接）。
- .env.example：环境变量示例（已在根目录保留符号链接）。
- init_roles.sql：数据库初始化角色脚本（供 docker-compose 使用）。
- db_schema_and_data.sql：数据库结构与数据快照（供初始化/备份）。
- insert_ai_config.sql：AI 配置初始化脚本。
- login_payload.json：登录测试载荷示例。
