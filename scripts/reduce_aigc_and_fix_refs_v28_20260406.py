# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (c) 2026 林志荣

from __future__ import annotations

from pathlib import Path

from docx import Document


SRC = Path(r"C:\Users\Twist\Desktop\论文\主稿\毕业论文初稿v2.8_降风险终修版_2026-04-06.docx")
TARGET = Path(r"C:\Users\Twist\Desktop\论文\主稿\毕业论文初稿v2.8_降风险与引用修订版_2026-04-06.docx")


def set_text(paragraph, text: str) -> None:
    paragraph.text = text


def main() -> None:
    doc = Document(str(SRC))
    p = doc.paragraphs

    replacements: dict[int, str] = {
        31: "围绕当前实现范围，本设计的研究内容主要集中在三条主线。第一条主线是把人事管理与物料管理两块基础业务做实，使员工、角色、物料、库存和出入库链路能够稳定运行。第二条主线是采用 PostgreSQL 与 PostgREST 组织接口和核心处理函数，把常用业务约束、状态处理和部分权限控制放在数据库侧统一完成。第三条主线是在此基础上接入微前端、流程配置、轻量语义增强和移动端场景，为后续扩展保留统一入口。",
        32: "从当前完成情况看，本设计已把人事、物料、应用中心和移动端等核心模块落到可演示、可联调的状态，流程设计与语义增强也已形成基础链路。换句话说，本说明书讨论的不只是方案设想，还包括一套已经能够运行、验证并继续扩展的实现底座。",
        33: "如图1-1所示，本设计的研究路径不是简单复述目录，而是把企业约束、需求分析、总体设计、关键技术路线、系统实现和验证关系压到同一条主线上。通过这张图，可以更直观地看出南派食品的问题如何逐步收敛到 EISCore 的设计与实现。",
        37: "本设计共分八章，各章安排如下。",
        38: "第一章交代研究背景、研究意义以及当前研究现状。第二章说明系统开发涉及的关键技术和工具。第三章围绕用户角色、业务流程和非功能要求展开系统需求分析。第四章给出系统概要设计，重点说明架构、能力组织和关键设计思路。第五章介绍数据库设计与语义增强相关设计。第六章按实现链路展开系统详细设计。第七章给出系统实现、测试情况以及当前边界说明。",
        39: "本设计的特点与创新点",
        40: "与单纯围绕页面堆叠展开的常见毕业设计相比，本设计更强调在真实业务约束下建立一套可以继续扩展的企业信息化底座。",
        41: "第一，本设计没有沿用常见的“前端 + 独立后端 + ORM 服务层”路线，而是尝试以数据库中心架构承载核心数据和部分业务处理，再通过自动接口映射向前端提供服务。第二，本设计没有把流程、权限和语义增强拆成互不相干的附加模块，而是放到同一底座内协同考虑。第三，本设计把移动端冷库模式、闪念应用和受控运行时都放在当前实现边界内讨论，使系统既能支撑现有业务，又为后续扩展留下了结构位置。",
        42: "总体概述",
        44: "本系统前端总体采用 Vue 3 与 Vite 的组合。这样选并不是为了追求概念新，而是因为当前项目包含大量表单录入、列表展示、弹窗交互和状态切换页面，继续依赖传统脚本堆叠会让页面很快失去可维护性。组件化前端框架更适合把这些交互拆成稳定、可复用的视图单元。",
        49: "在前端组织方式上，本设计选用了 qiankun 作为微前端框架。原因并不复杂：企业信息化系统的功能入口不会长期固定，随着业务扩展，模块、页面和入口都可能持续增加。微前端提供的是边界清晰的组织方式，使不同功能域在保持统一入口的同时仍能相对独立地演进。至于这些子应用怎样和应用中心、流程配置及移动端协同，后文会在系统概要设计中继续展开。",
        50: "界面组件与交互支撑技术",
        51: "企业信息化系统最常见的页面并不是展示页，而是表格、表单、审批视图、统计面板和移动端录入页。基于这一点，桌面端主要选用 Element Plus 作为基础组件库，移动端选用 Vant 作为主要组件支撑。这样做的目的是减少重复封装工作，并把页面风格控制在同一套可维护的范围内。",
        299: "用户表 public.users 的主要字段列于表5-1。该表承担账号信息、基础身份属性和组织归属信息的存储功能，是系统登录与权限识别的重要入口。",
        301: "角色表 public.roles 的主要字段列于表5-2。该表用于定义系统角色编码、角色名称以及角色说明信息，并可与部门形成关联，为后续权限分配和流程节点候选角色配置提供基础。",
        303: "用户角色关联表 public.user_roles 的字段列于表5-3。该表用于建立用户与角色之间的多对多关系，使系统能够支持一个用户对应多个角色，满足审批、仓储和管理查看等复合授权场景。",
        305: "物料主数据表 public.raw_materials 的主要字段列于表5-4。该表虽然命名为原料表，但在当前系统实现中承担了较广义的物料基础信息存储职责，并通过 properties 字段兼容扩展属性。",
        307: "仓库表 scm.warehouses 的主要字段列于表5-5。该表采用树形结构设计，可同时表示仓库、库区和库位三个层级，适合食品企业中分区管理和现场布局扩展的实际需要。",
        309: "库存批次表 scm.inventory_batches 的字段列于表5-6。该表是本系统中最重要的仓储业务表之一，主要用于保存某种物料在某个仓位上的批次库存状态，为批次追溯、保质期管理和台账统计提供支撑。",
        311: "库存流水表 scm.inventory_transactions 的字段列于表5-7。该表记录所有出入库和库存变化行为，是连接库存状态与业务动作的核心表。系统通过该表保存单据号、业务类型、数量变化和关联单据信息，为库存审计和问题追溯提供依据。",
        313: "应用注册表 app_center.apps 的字段列于表5-8。该表用于保存应用中心中的应用定义信息，同时支持数据应用、流程应用、Flash 草稿应用和自定义应用，是动态应用管理能力的核心载体。",
        315: "流程实例表 workflow.instances 的字段列于表5-9。该表用于保存流程运行态数据，是系统流程引擎与业务状态联动的核心表之一。与只保存设计稿的流程定义表不同，该表更关注具体业务对象当前运行到哪个任务节点以及实例状态如何变化。",
        391: "流程实例启动函数的输入、输出与职责列于表6-4。该表说明业务页面发起流程后，数据库侧如何根据流程定义、业务主键和初始任务节点生成实例记录。",
        393: "流程节点执行权限判断函数的输入、输出与职责列于表6-5。该表重点解释“当前审批人是否具备执行资格”这一判断如何在数据库侧完成，并与任务分派和 JWT 声明保持一致。",
        400: "流程启动与状态写回的时序如图6-3所示。用户先从业务页面或应用中心发起流程，前端提交流程定义编号、业务键和运行变量，再由 workflow.start_workflow_instance 创建流程实例并写入初始状态。后续审批推进时，workflow.transition_workflow_instance 负责更新实例状态、写入审批日志，并根据状态映射关系回写业务表。见图6-3。",
        495: "本次测试环境的基础配置列于表8-1。",
        497: "其中，系统入口通过宿主应用访问，API 通过 /api 代理访问数据库接口，应用中心辅助运行能力通过 /agent 路径代理访问运行组件。",
        511: "登录功能黑盒测试结果列于表8-2。该组测试说明系统当前已经具备基础登录鉴权能力，能够区分合法用户和非法访问请求。",
        514: "页面访问与路由测试结果列于表8-3。该组测试主要验证微前端场景下最容易出现问题的深链刷新能力，结果说明当前基座和子应用路由协同已基本稳定。",
        517: "核心数据接口测试结果列于表8-4。该组测试说明数据库中心架构下的核心接口访问链路已经能够正常工作，为人事、物料和应用中心等模块的页面运行提供了基础支撑。",
        520: "辅助运行能力测试结果列于表8-5。这一组测试说明辅助运行组件当前已经能在基础权限控制下正常提供服务。",
        523: "运行状态测试结果列于表8-6。该组测试从运行环境角度验证了系统的基础可用性，说明当前项目已经具备持续联调和展示条件。",
    }

    for idx, text in replacements.items():
        if idx < len(p):
            set_text(p[idx], text)

    # Ensure explicit figure references that were malformed or missing.
    for para in p:
        text = para.text.strip()
        if text == "见":
            para.text = "见图3-4。"
        elif text.endswith("见图6-1。") and "图6-2强调" in text:
            para.text = text[:-6] + "见图6-2。"
        elif text.endswith("见图6-4"):
            para.text = text + "。"

    TARGET.parent.mkdir(parents=True, exist_ok=True)
    doc.save(str(TARGET))
    print(TARGET)


if __name__ == "__main__":
    main()
